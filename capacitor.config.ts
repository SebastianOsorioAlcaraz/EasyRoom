import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Easy Room Front End Nuevo',
  webDir: 'www'
};

export default config;
